services:
  tefca-viewer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
  tefca-fhir-server:
    image: "hapiproject/hapi:latest"
    ports:
      - "8080:8080"