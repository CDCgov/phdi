{
  "name": "fhir-converter",
  "scripts": {
    "dev": "docker run --rm -it -p 8080:8080 -e DEV_MODE=true $(docker build -q .)",
    "build-loud": "DOCKER_BUILDKIT=0 docker build . --no-cache",
    "test:dotnet": "dotnet test",
    "dev-loud": "docker run --rm -it -p 8080:8080 -e DEV_MODE=true $(docker build -q .)"
  }
}
